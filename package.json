{
  "name": "craftdefense",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.emit.json && node scripts/dev.mjs",
    "build": "tsc -p tsconfig.emit.json && node scripts/build.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.165.0",
    "three-mesh-bvh": "^0.9.9",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "devDependencies": {
    "@types/three": "^0.184.1"
  }
}
